<div class="container">
<h1>Add a list to our database</h1>
  <%= simple_form_for @list do | f | %>
    <%= f.input :name %>
    <%= f.submit "Add lists", class: "btn btn-primary"%>
  <% end %>
  <%= link_to "cancel", lists_path(@lists) %>
</div>
